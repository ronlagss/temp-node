const num1 = 5; 
const num2 = 10; 

function addValues () {
    console.log(`the sum is : ${num1 + num2}`); 
}
addValues(); //if there is a function executed then it will come up if called "require" somewhere.